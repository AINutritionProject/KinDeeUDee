import 'package:flutter/material.dart';

class VetABPage extends StatelessWidget {
  const VetABPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Vet A page")),
    );
  }
}
