import 'package:flutter/material.dart';

class VetBPage extends StatelessWidget {
  const VetBPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vet B')),
    );
  }
}
